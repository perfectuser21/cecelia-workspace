# collect Feature Configuration
name: collect
category: business
version: 1.0.0

# API Configuration
api:
  basePath: /v1/collect
  requiresAuth: true

# Dependencies
dependencies:
  shared:
    - middleware/error.middleware
    - utils/logger
    - types
  features:
    - accounts
    - metrics

# Worker Integration
workers:
  - douyin
  - xiaohongshu
  - kuaishou
  - bilibili
