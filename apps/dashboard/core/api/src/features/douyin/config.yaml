# douyin Feature Configuration
name: douyin
category: business
version: 1.0.0

# API Configuration
api:
  basePath: /douyin
  requiresAuth: false

# Dependencies
dependencies:
  shared:
    - utils/logger

# External Services
services:
  - VPS API (douyin-api)

# Environment Variables
env:
  - VPS_API_HOST
