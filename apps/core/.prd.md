# PRD - Command Center 路由修复

## 问题
Command Center 下钻导航 (/command/tasks, /command/projects 等) 在 Core 实例不工作，点击后回到首页。

## 原因
Core 的 panorama/index.ts 路由配置缺少通配符路由 `/command/*`。

## 修复
添加 `/command/*` 路由，让 CommandCenter 组件处理所有子路由。
