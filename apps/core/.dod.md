## DoD - Cecelia 静默层 Phase 1：基础修通

### 验收清单

#### Task 1.1: Scheduler 并发限制
- [ ] `MAX_CONCURRENT_TASKS` 从环境变量 `CECELIA_MAX_CONCURRENT` 读取
- [ ] 默认值为 3
- [ ] tick.js 中不再硬编码并发数

#### Task 1.2: tick 闭环修复
- [ ] executor.js 的 callback URL 指向 `http://localhost:5212/api/brain/execution-callback`
- [ ] 手动 POST /api/brain/tick 能成功派出任务
- [ ] 任务完成后 callback 能正确更新任务状态

#### Task 1.3: 决策引擎阈值
- [ ] 目标偏差阈值从 -20% 降到 -10%
- [ ] 增加兜底逻辑：有 failed 任务时生成 retry action
- [ ] POST /api/brain/goal/compare 返回非空 recommendations

#### 不退化检查
- [ ] `npx vitest run src/brain/` 全部通过（217+ tests）
- [ ] TypeScript 类型检查通过

### QA 决策
- 参考 docs/QA-DECISION.md
