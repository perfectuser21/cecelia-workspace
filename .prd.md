# PRD - 验证 Goal API 创建/查询功能

## 需求来源

任务来自 Brain 自动调度，Task ID: 29ed8180-f852-4e24-943d-45dea10fdae2

## 功能描述

测试 OKR 系统的 Goal API：
- 创建 Goal
- 查询 Goal 列表
- 更新 Goal 状态
- 删除 Goal

验证 API 响应正确性和数据持久化。

## Task Type

**qa** - 这是一个 QA 验证任务，不是功能开发。

## 涉及文件

API 端点（已存在，需要验证）：
- `POST /api/brain/goals` - 创建 Goal
- `GET /api/brain/goals` - 查询 Goals
- `PUT /api/brain/goals/:id` - 更新 Goal
- `DELETE /api/brain/goals/:id` - 删除 Goal

测试文件（可能需要创建）：
- `apps/core/src/__tests__/goals-api.test.ts` - Goal API 集成测试

## 成功标准

- [ ] 创建 Goal API 返回正确响应
- [ ] 查询 Goals 列表包含新创建的 Goal
- [ ] 更新 Goal 状态成功
- [ ] 删除 Goal 成功
- [ ] 数据持久化验证（刷新后数据不丢失）

## 非目标

- 不修改 Goal API 实现
- 不新增 API 功能
- 只做验证和测试
