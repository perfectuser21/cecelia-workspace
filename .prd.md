# 模型显示名称优化

## 功能描述
优化 Agent 模型配置表格中的模型显示方式：移除冗余 provider 文字标签，用颜色圆点替代。后端模型名称去掉 provider 前缀，区分同 provider 的不同变体。

## 成功标准
- 模型按钮只显示模型名（如 M2.5），不再显示 provider 文字
- 用颜色圆点指示 provider（紫=Anthropic，绿=MiniMax，蓝=OpenAI）
- M2.5 和 M2.5 Fast 能明确区分
- 下拉菜单保留 provider 分组标题
