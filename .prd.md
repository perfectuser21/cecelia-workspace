## PRD - 侧边栏精简：XX logo + 文字标题 + 删除用户区

**需求来源**: 单用户系统不需要底部用户区域，收起 logo 改为 XX，展开用 Perfect21 文字替代图片。

**功能描述**:
1. config.ts logoCollapsed 从 P 改为 XX
2. App.tsx 和 DynamicSidebar.tsx 展开时 logo 从 img 改为 Perfect21 文字
3. App.tsx 删除底部用户区域（头像+Admin+超级管理员+System）
4. DynamicSidebar.tsx 删除底部用户区域 + 清理 onLogout prop 和 LogOut import

**涉及文件**:
- `apps/core/features/config.ts`
- `apps/dashboard/src/App.tsx`
- `apps/dashboard/src/components/DynamicSidebar.tsx`

**成功标准**:
1. `npx vite build` 编译通过
2. 收起显示 XX，展开显示 Perfect21 文字
3. 侧边栏底部无用户区域

**非目标**:
- 不改后端
- 不改 AuthContext（login/logout 接口保留）
