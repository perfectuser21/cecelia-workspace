# PRD: 清理 workspace 旧 Brain 代码

## 背景
Brain 已经迁移到 cecelia-semantic-brain（独立服务，端口 5221），但 workspace 的 `apps/core/src/brain/` 目录仍然存在，导致：
1. 代码分叉（昨天 20+ PR 错误地修改了旧代码）
2. 维护混乱（两处代码不同步）
3. 架构不清晰（应该只调用 API）

## 目标
删除 workspace 的旧 Brain 代码，改用 semantic-brain API。

## 实现方案

### 1. 修改 API 调用
文件：`apps/core/src/dashboard/routes.ts`
- 移除 brain import
- 创建 Brain API 客户端函数
- 调用 semantic-brain 的 `/intent/parse` 和 `/intent/create` 端点

### 2. 删除旧代码
- 删除 `apps/core/src/brain/` 目录
- 确认没有其他地方引用

### 3. 验证
- 运行测试
- 测试 intent 识别功能

## 验收标准
- ✅ routes.ts 改用 API 调用
- ✅ brain 目录已删除
- ✅ 测试通过
- ✅ 功能正常
