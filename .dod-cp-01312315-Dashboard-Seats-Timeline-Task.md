## DoD - Dashboard 实时更新：Seats + Timeline + Task Chain

### 验收清单

- [ ] **SSE Backend**: `GET /api/cecelia/stream` 返回 `text/event-stream`，每 5s 推送 JSON 数据
- [ ] **SSE Data**: 推送包含 `seats`（槽位状态）和 `overview`（任务概览）两个字段
- [ ] **useSSE Hook**: 创建 `useSSE.ts`，自动连接 SSE，支持断连重连（3s）和轮询降级（30s）
- [ ] **Seats 组件**: 显示每个并发槽位的状态（空闲/运行中）、任务名、运行时长
- [ ] **Timeline 组件**: 显示最近任务的 checkpoint 进度条
- [ ] **TaskChain 组件**: 显示当前运行中任务的 11 步骤详情
- [ ] **TypeCheck**: `npm run typecheck --workspace=apps/core` 通过
- [ ] **Build**: `npm run build --workspace=apps/core` 通过

### QA 决策

- 测试类型：手动验证 SSE 连接 + TypeCheck + Build
- 无需单元测试（SSE 为实时流，手动验证更有效）
