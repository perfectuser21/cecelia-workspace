# PRD - Cecelia Agent System

> Cecelia 管家系统 - 在 Core 前端展示 Cecelia 及其底下的 Agent 能力

---

## 元信息

| 字段 | 值 |
|------|-----|
| 项目 | `zenithjoy-core` |
| 功能分支 | `cp-*-cecelia-agents` |
| 创建时间 | `2026-01-19 22:30` |
| 状态 | `draft` |

---

## 背景

Cecelia 是 Core 的 AI 管家，类似 Autopilot 的 AI 员工系统，但定位不同：
- **Autopilot AI 员工**：团队运营，多部门多员工
- **Cecelia**：个人管家，底下有多个 Agent 执行不同能力

目前 Core 的 Cecelia 页面只有一个简单的任务列表（CeceliaRuns.tsx），没有展示 Cecelia 的架构和能力。

## 目标

1. 在 Core 前端展示 Cecelia 管家系统的完整架构
2. 展示 Cecelia 底下的 Agents 及其能力
3. 每个 Agent 关联对应的 N8N 工作流，显示执行状态

## 非目标

- 不修改 N8N 工作流本身
- 不实现新的后端 API（复用现有 n8n-live-status API）
- 不修改 Autopilot 的 AI 员工系统

---

## 功能描述

### Cecelia 架构

```
Cecelia（管家）
├── Spark   - 执行者 - 点燃代码执行
│   └── N8N: PRD Executor
├── Echo    - 协调者 - 多阶段反馈协调
│   └── N8N: Coordinator
└── Prism   - 分析者 - 多维分析决策（预留）
    └── N8N: (未来扩展)
```

### Agent 配置

```typescript
interface CeceliaAgent {
  id: string;           // spark, echo, prism
  name: string;         // Spark, Echo, Prism
  codename: string;     // 代号描述
  icon: string;         // Lucide 图标名
  role: string;         // 角色定位
  description: string;  // 能力描述
  workflowKeywords: string[];  // 匹配 N8N workflow 名称
  status: 'active' | 'standby' | 'planned';
}
```

### 页面结构

#### 1. Cecelia 总览页 `/cecelia`

```
┌─────────────────────────────────────────────────────┐
│  Cecelia                                            │
│  Your AI Butler · 3 Agents · 今日 12 次任务         │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐             │
│  │  Spark  │  │  Echo   │  │  Prism  │             │
│  │   ⚡    │  │   🔄    │  │   💎    │             │
│  │ 执行者  │  │ 协调者  │  │ 分析者  │             │
│  │         │  │         │  │         │             │
│  │ 今日 8  │  │ 今日 4  │  │ 待激活  │             │
│  │ 成功 7  │  │ 成功 4  │  │         │             │
│  └─────────┘  └─────────┘  └─────────┘             │
│                                                     │
├─────────────────────────────────────────────────────┤
│  最近任务                                           │
│  ├─ [✓] Spark · PRD 执行 · zenithjoy-core · 10:30  │
│  ├─ [✓] Echo  · 阶段协调 · zenithjoy-core · 10:25  │
│  └─ [⟳] Spark · PRD 执行 · zenithjoy-engine · now  │
└─────────────────────────────────────────────────────┘
```

#### 2. Agent 详情页 `/cecelia/agents/:agentId`

```
┌─────────────────────────────────────────────────────┐
│  ← 返回                                             │
├─────────────────────────────────────────────────────┤
│  ⚡ Spark                                           │
│  执行者 · "点燃代码执行"                            │
│                                                     │
│  能力描述：                                         │
│  解析 PRD 文档，拆解 Checkpoints，                  │
│  调用 Claude Code 执行代码任务                      │
│                                                     │
│  关联工作流：PRD Executor                           │
├─────────────────────────────────────────────────────┤
│  今日统计                                           │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │
│  │ 任务 8 │ │ 成功 7 │ │ 失败 1 │ │ 87.5%  │       │
│  └────────┘ └────────┘ └────────┘ └────────┘       │
├─────────────────────────────────────────────────────┤
│  执行历史                                           │
│  ├─ [✓] zenithjoy-core · cp-xxx · 10:30 · 5m       │
│  ├─ [✗] zenithjoy-engine · cp-xxx · 09:15 · 3m     │
│  └─ ...                                             │
└─────────────────────────────────────────────────────┘
```

### 技术方案

1. **配置文件**：`features/cecelia/config/agents.config.ts`
   - 定义 Spark, Echo, Prism 三个 Agent
   - 每个 Agent 的 workflowKeywords 用于匹配 N8N 执行记录

2. **API 层**：`features/cecelia/api/agents.api.ts`
   - 复用 `/api/v1/n8n-live-status` 获取执行数据
   - 按 Agent 的 workflowKeywords 聚合统计

3. **页面组件**：
   - `CeceliaOverview.tsx` - 总览页（替代现有 CeceliaRuns.tsx）
   - `AgentCard.tsx` - Agent 卡片组件
   - `AgentDetail.tsx` - Agent 详情页

4. **路由更新**：
   - `/cecelia` → CeceliaOverview
   - `/cecelia/agents/:agentId` → AgentDetail
   - `/cecelia/runs` → 保留原有任务列表（可选）

---

## Checkpoints

- [ ] CP-001: 创建 Agent 配置文件 | config | none
- [ ] CP-002: 实现 Agent API 层 | code | CP-001
- [ ] CP-003: 实现 CeceliaOverview 页面 | code | CP-002
- [ ] CP-004: 实现 AgentCard 组件 | code | CP-002
- [ ] CP-005: 实现 AgentDetail 页面 | code | CP-004
- [ ] CP-006: 更新路由配置 | config | CP-005
- [ ] CP-007: 验证页面效果 | test | CP-006

### Checkpoint 详情

#### CP-001: 创建 Agent 配置文件

**类型**: `config`
**依赖**: `none`

**任务描述**:
创建 `features/cecelia/config/agents.config.ts`，定义三个 Agent：

```typescript
export const CECELIA_AGENTS: CeceliaAgent[] = [
  {
    id: 'spark',
    name: 'Spark',
    codename: '点燃执行',
    icon: 'Zap',
    role: '执行者',
    description: '解析 PRD 文档，拆解 Checkpoints，调用 Claude Code 执行代码任务',
    workflowKeywords: ['PRD Executor', 'prd-executor', 'Execute'],
    status: 'active',
  },
  {
    id: 'echo',
    name: 'Echo',
    codename: '协调反馈',
    icon: 'RefreshCw',
    role: '协调者',
    description: '监控执行状态，处理多阶段任务，分析结果并创建下一阶段',
    workflowKeywords: ['Coordinator', 'coordinator', 'Multi-stage'],
    status: 'active',
  },
  {
    id: 'prism',
    name: 'Prism',
    codename: '多维分析',
    icon: 'Diamond',
    role: '分析者',
    description: '多维度分析执行结果，提供智能决策建议',
    workflowKeywords: ['Analyzer', 'Analysis'],
    status: 'planned',
  },
];
```

---

#### CP-002: 实现 Agent API 层

**类型**: `code`
**依赖**: `CP-001`

**任务描述**:
创建 `features/cecelia/api/agents.api.ts`：
- 复用 n8n-live-status API 获取执行数据
- 按 Agent 的 workflowKeywords 聚合
- 返回每个 Agent 的统计和最近任务

---

#### CP-003: 实现 CeceliaOverview 页面

**类型**: `code`
**依赖**: `CP-002`

**任务描述**:
创建 `features/cecelia/pages/CeceliaOverview.tsx`：
- 显示 Cecelia 管家信息
- 展示三个 Agent 卡片
- 显示最近任务列表
- 30 秒自动刷新

---

#### CP-004: 实现 AgentCard 组件

**类型**: `code`
**依赖**: `CP-002`

**任务描述**:
创建 `features/cecelia/components/AgentCard.tsx`：
- 显示 Agent 图标、名称、角色
- 显示今日统计（任务数、成功数）
- 点击跳转到详情页
- planned 状态显示"待激活"

---

#### CP-005: 实现 AgentDetail 页面

**类型**: `code`
**依赖**: `CP-004`

**任务描述**:
创建 `features/cecelia/pages/AgentDetail.tsx`：
- 显示 Agent 完整信息
- 今日统计卡片
- 执行历史列表

---

#### CP-006: 更新路由配置

**类型**: `config`
**依赖**: `CP-005`

**任务描述**:
更新 `features/cecelia/index.ts` 的路由配置：
- `/cecelia` → CeceliaOverview
- `/cecelia/agents/:agentId` → AgentDetail

---

#### CP-007: 验证页面效果

**类型**: `test`
**依赖**: `CP-006`

**任务描述**:
- 启动开发服务器
- 访问 `/cecelia` 验证总览页
- 点击 Agent 卡片验证跳转
- 验证数据刷新

---

## 验收标准

1. **架构清晰** - Cecelia 管家 + 3 个 Agent 的层级结构展示
2. **数据联动** - Agent 统计数据来自 N8N 执行记录
3. **交互完整** - 总览 → Agent 详情的导航流畅
4. **风格统一** - 与现有 Core 界面风格一致（深色主题、渐变卡片）

---

## 风险与依赖

| 风险/依赖 | 影响 | 缓解措施 |
|----------|------|---------|
| N8N API 可能无数据 | 页面显示空 | 添加空状态提示 |
| workflowKeywords 匹配不准 | 统计错误 | 预留配置调整能力 |

---

## UI 参考

参考 Autopilot 的 AI 员工页面风格，但更简洁：
- 卡片式布局
- 深色主题 + 渐变色
- Lucide 图标
