# DoD - OKR 页面去重：提取共享组件

QA: docs/QA-DECISION.md

## 验收标准

### 共享组件验收

- [ ] ProgressBar 组件已创建
      Test: bash:test -f apps/core/features/shared/components/ProgressBar.tsx && echo PASS
- [ ] StatusIcon 组件已创建
      Test: bash:test -f apps/core/features/shared/components/StatusIcon.tsx && echo PASS
- [ ] PriorityBadge 组件已创建
      Test: bash:test -f apps/core/features/shared/components/PriorityBadge.tsx && echo PASS

### 页面引用验收

- [ ] OKRPage 从 shared 导入组件
      Test: bash:grep "shared/components" apps/core/features/planning/pages/OKRPage.tsx && echo PASS
- [ ] OrchestratorPage 从 shared 导入组件
      Test: bash:grep "shared/components" apps/core/features/execution/pages/OrchestratorPage.tsx && echo PASS

### 编译验收

- [ ] Vite 前端构建通过
      Test: bash:cd apps/dashboard/frontend && npx vite build
