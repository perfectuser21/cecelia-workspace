# DoD - 仓库清理：对齐代码与文件状态

QA: docs/QA-DECISION.md

## 验收标准

### 文件清理验收

- [ ] 根目录无历史 `.prd-*.md` 和 `.dod-cp-*.md` 文件
      Test: bash:ls .prd-*.md .dod-cp-*.md 2>/dev/null | wc -l 应为 0
- [ ] `dashboard/` 目录已删除
      Test: bash:test ! -d dashboard && echo PASS
- [ ] `.git-rewrite/` 目录已删除
      Test: bash:test ! -d .git-rewrite && echo PASS
- [ ] `apps/core/features/node_modules` 已删除
      Test: bash:test ! -d apps/core/features/node_modules && echo PASS
- [ ] 垃圾文件已清理（.dev-mode.lock, .prd.md~develop, backup 文件, gate 标记）
      Test: bash:ls .dev-mode.lock .prd.md~develop .gate-*-passed 2>/dev/null | wc -l 应为 0

### Feature 模块对齐验收

- [ ] planning/index.ts 所有声明的组件文件都存在
      Test: bash:验证 planning 声明的每个组件路径存在
- [ ] execution/index.ts 所有声明的组件文件都存在
      Test: bash:验证 execution 声明的每个组件路径存在
- [ ] system/index.ts 所有声明的组件文件都存在
      Test: bash:验证 system 声明的每个组件路径存在

### 编译验收

- [ ] Vite 前端构建通过
      Test: npx vite build
- [ ] 现有功能不受影响（删除的只是不存在文件的声明）
      Test: manual:确认删除的路由/组件声明都是对应文件不存在的
