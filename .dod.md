---
id: dod-workspace-generic-credentials
version: 1.0.0
created: 2026-02-25
updated: 2026-02-25
changelog:
  - 1.0.0: 初始版本
---

# DoD: TeamPage 通用 Credentials 支持

## 验收清单

- [x] `MINIMAX_ACCOUNTS` 已替换为 `PROVIDER_ACCOUNTS: Record<string, string[]>`
- [x] `PROVIDER_ACCOUNTS` 包含 anthropic、minimax、openai 三个 provider 的账户列表
- [x] WorkerPanel `credentialsFile` 初始值改为 `credentials_file || provider || 'anthropic'`
- [x] save 函数移除 `provider === 'minimax' ?` 条件，对所有 provider 传 `credentials_file`
- [x] provider select onChange 时同步更新 `credentialsFile` 为新 provider 默认值
- [x] 账户下拉移除 `provider === 'minimax' &&` 条件，对所有 provider 显示
- [x] `tsc --noEmit` 无类型错误
