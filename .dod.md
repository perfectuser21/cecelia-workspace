# DoD - TaskDatabase 丝滑度优化（P0）

## 验收标准

### 功能验收

- [ ] 拖拽手柄默认 `opacity-0`，鼠标悬停行时显示 `group-hover:opacity-100`
      Test: manual:浏览器 hover 表格行验证拖拽手柄显示
- [ ] Table 视图拖拽时卡片有 `scale(1.02)` 放大效果
      Test: manual:拖拽行验证 scale 动画
- [ ] Board 视图拖拽时卡片有 `scale(1.02)` 放大效果
      Test: manual:拖拽看板卡片验证 scale 动画
- [ ] 表格行 padding 从 `py-2.5` 增加到 `py-3`
      Test: manual:视觉检查行高舒适度
- [ ] Priority 下拉菜单有淡入 + 上滑动画（200ms）
      Test: manual:点击 Priority 标签验证动画
- [ ] Status 下拉菜单有淡入 + 上滑动画（200ms）
      Test: manual:点击 Status 标签验证动画

### 测试验收

- [ ] vite build 通过，无 TypeScript 编译错误
      Test: contract:vite-build
