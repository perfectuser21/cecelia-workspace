# DoD - OKR Task Tree 交互编辑

## 验收标准

### 功能验收

- [ ] 点击任务的 Priority 标签（P0/P1/P2）弹出下拉选择器，选择后调用 PATCH /api/tasks/tasks/:id 更新
      Test: manual:浏览器访问 /work/tasks 点击优先级标签验证
- [ ] 点击任务的 Status 标签弹出下拉选择器，选择后调用 PATCH /api/tasks/tasks/:id 更新
      Test: manual:浏览器访问 /work/tasks 点击状态标签验证
- [ ] 点击目标的 Priority 标签弹出下拉选择器，选择后调用 PATCH /api/tasks/goals/:id 更新
      Test: manual:浏览器访问 /work/tasks 点击目标优先级验证
- [ ] 更新失败时 UI 回滚并显示错误提示
      Test: manual:断网或无效 ID 测试回滚行为
- [ ] 点击选择器外部区域关闭选择器
      Test: manual:点击空白区域验证关闭

### 测试验收

- [ ] vite build 通过，无 TypeScript 编译错误
      Test: contract:vite-build
