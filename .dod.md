# DoD - Ops Dashboard + OKR Task Tree

## 验收标准

### 功能验收

- [ ] OpsDashboard 组件渲染不报错，显示 VPS 双列对比卡片
      Test: manual:浏览器访问 /system 验证 Ops tab 渲染
- [ ] OpsDashboard 调用 Brain API 端点并展示数据（health, cluster, vps-slots, tick, alertness, circuit-breaker, token-usage, watchdog）
      Test: manual:截图验证数据展示
- [ ] OKRTaskTree 组件渲染不报错，显示 Objective → KR → Task 树形结构
      Test: manual:浏览器访问 /work/tasks 验证树形渲染
- [ ] OKRTaskTree 支持展开/折叠 Objective 和 KR 节点
      Test: manual:点击展开折叠验证
- [ ] OKRTaskTree 支持按 Priority 和 Status 筛选
      Test: manual:筛选操作验证
- [ ] SystemTabbed 第一个 tab 是 Ops，其他 tab 顺序不变
      Test: manual:截图验证 tab 顺序
- [ ] WorkTabbed 新增 Tasks tab，路由 /work/tasks 正常工作
      Test: manual:导航验证

### 测试验收

- [ ] vite build 通过，无 TypeScript 编译错误
      Test: contract:vite-build
