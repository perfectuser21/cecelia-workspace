# DoD: 修复 Inbox WebSocket 连接路径

## 验收清单

- [ ] `server.ts` 中新增 `brainWsProxy`（无 pathRewrite），专用于 WebSocket upgrade
  - Test: 代码审查确认新增代理对象不含 pathRewrite
- [ ] WebSocket upgrade handler 改用 `brainWsProxy`，`req.url` 保持 `/ws` 不改写
  - Test: 代码审查确认 upgrade handler 不再手动改写 req.url
- [ ] HTTP 路由 `app.use('/api/brain', brainProxy)` 保持不变
  - Test: `curl http://localhost:5212/api/brain/health` 返回 200
- [ ] npm test 通过（所有测试文件）
  - Test: `cd apps/core && npm test` 退出码 0
- [ ] 代码构建通过
  - Test: `cd apps/core && npm run build` 无 TypeScript 编译错误
