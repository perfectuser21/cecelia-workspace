---
id: dod-python-semantic-api-v1
version: 1.0.0
created: 2026-01-31
updated: 2026-01-31
changelog:
  - 1.0.0: 初始版本
---

# DoD - Python Semantic API v1

QA: docs/QA-DECISION.md

## 验收标准

### 功能验收
- [x] POST /v1/semantic/embed 端点存在，接收 text 字段，返回 embedding 向量、model 和 dimensions
      Test: manual:curl POST http://localhost:5220/v1/semantic/embed 返回 200 和 embedding 数组
- [x] POST /v1/semantic/search 端点存在，接收 query/top_k/filters，返回 results 列表和 query_time_ms
      Test: manual:curl POST http://localhost:5220/v1/semantic/search 返回 200 和 results 数组
- [x] POST /v1/semantic/rerank 端点存在，接收 query 和 results 列表，返回重排序后的 results
      Test: manual:curl POST http://localhost:5220/v1/semantic/rerank 返回 200
- [x] GET /v1/semantic/health 返回 status、embedder 状态和 store 状态
      Test: manual:curl GET http://localhost:5220/v1/semantic/health 返回 200 和 status 字段
- [x] 四个端点在依赖未初始化时返回 503，空输入时返回 400
      Test: manual:验证空 text/query 返回 400 错误
