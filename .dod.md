# DoD - Notion-like Task Database View

## 验收标准

### 功能验收

- [ ] `/work/tasks` 默认显示 Table 视图，包含拖拽手柄、Title、Status、Priority、Project、Type、Created 列
      Test: manual:浏览器访问 /work/tasks 验证表格列显示
- [ ] Table 视图列头可点击排序（升序/降序切换）
      Test: manual:点击列头验证排序箭头和数据顺序
- [ ] 切换 Board 视图显示三列看板（Queued / In Progress / Completed），拖拽卡片跨列改 status
      Test: manual:切换 Board 视图拖拽卡片验证状态更新
- [ ] Group By 切换（None/Status/Priority/Project/Month）表格按分组折叠显示
      Test: manual:切换分组验证折叠和内容正确
- [ ] Board 视图下 Group By 自动禁用
      Test: manual:切换 Board 视图验证 Group By 下拉禁用
- [ ] 点击 Priority/Status 标签内联编辑，调用 PATCH API 更新，失败回滚
      Test: manual:点击标签验证下拉选择和 API 调用
- [ ] Table 拖拽行重排序，刷新后顺序保持（localStorage）
      Test: manual:拖拽行后刷新页面验证顺序保持
- [ ] Quarantine section 和 Focus badge 正常显示
      Test: manual:验证隔离区和焦点标识显示

### 测试验收

- [ ] vite build 通过，无 TypeScript 编译错误
      Test: contract:vite-build
