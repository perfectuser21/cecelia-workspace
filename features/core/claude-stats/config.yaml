# Claude Stats Feature Configuration
name: claude-stats
version: "1.0.0"
description: Claude 使用统计（Token/Cost）

api:
  basePath: /v1/claude-stats
  requiresAuth: false

dependencies:
  shared: []  # 无外部依赖

endpoints:
  - GET /v1/claude-stats
